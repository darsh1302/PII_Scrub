# PII Scrubbing Agent — pinned dependencies
#
# Every dependency is exact-pinned. This is a CORRECTNESS requirement, not
# housekeeping: engine and model versions are recorded in every ProcessingResult
# and AuditRecord (Requirement 46.7, guardrail G21). An unpinned upgrade would
# silently change detection output and make a historical compliance claim
# non-reproducible.
#
# tests/test_dependency_pins.py enforces that every line here is exact-pinned.

# --- Agent orchestration ---
langgraph==1.2.11
langchain==1.3.15
langchain-openai==1.5.1
openai==3.1.0
tiktoken==0.13.0

# --- Detection engines (version recorded in audit trail) ---
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
spacy==3.8.15
# spaCy models pinned by wheel URL — `spacy download` resolves a floating version.
# _lg is the default and the reference environment for recorded results.
# _sm is only for memory-constrained deployments via PII_AGENT_SPACY_MODEL; it is
# ~12 MB and detects noticeably fewer personal names, which the health panel and
# the audit record both state explicitly.
en_core_web_lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# --- Hardened parsing (guardrail G12: XXE / entity expansion) ---
defusedxml==0.7.1

# --- Configuration ---
python-dotenv==1.2.2
PyYAML==6.0.3

# --- Sources ---
boto3==1.42.6
pywin32==312; sys_platform == "win32"

# --- UI ---
streamlit==1.61.1

# --- Test tooling ---
pytest==9.0.1
pytest-cov==7.1.0
hypothesis==6.140.3
