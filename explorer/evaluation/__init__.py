"""Datasets, metrics and regression comparison. Deferred beyond the MVP.

An evaluation result records the configuration and component versions that produced
it, so a comparison across versions is identifiable as such rather than silently
invalid.
"""
